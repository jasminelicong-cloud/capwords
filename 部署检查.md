# Vercel 部署检查清单

## 📋 问题诊断

### 1️⃣ 部署失败的可能原因

#### API 错误
```
❌ 错误信息: "请求参数错误" or "Missing bot_app_key"
✅ 解决方案:
   - 检查 BOT_APP_KEY 是否在 Vercel 环境变量中配置
   - 或者在前端手动输入 BOT_APP_KEY
```

#### 导入错误
```
❌ 错误信息: "Cannot find module 'node-fetch'"
✅ 解决方案:
   - 已移除 node-fetch 依赖
   - 使用 Node.js 18+ 原生 fetch 
   - 确保 package.json 中没有 node-fetch
```

#### 环境配置错误
```
❌ 错误信息: "upload-image API 返回 500"
✅ 解决方案:
   - 确保 SECRET_ID, SECRET_KEY, BOT_BIZ_ID 已配置
   - 检查腾讯云 SDK 是否已安装
```

---

## 🚀 快速部署步骤

### 第一步：本地验证
```bash
cd /Users/sofiali/CodeBuddy/20251020190956/vercel-deploy
npm install --no-save
```

### 第二步：上传到 Vercel
```bash
vercel --prod
```

### 第三步：配置环境变量

在 https://vercel.com/dashboard 中：

**Settings → Environment Variables** 添加：

| 变量名 | 值 | 用途 |
|------|-----|------|
| `BOT_APP_KEY` | `DBIbjNFWBrr...` | 腾讯云 Bot API Key |
| `SECRET_ID` | `AKID...` | 腾讯云访问 ID |
| `SECRET_KEY` | `您的密钥` | 腾讯云访问密钥 |
| `BOT_BIZ_ID` | `1980136...` | Bot 业务 ID |

### 第四步：重新部署
```bash
vercel --prod
```

---

## 🔍 测试 API

### 测试健康检查（无需部署）
```bash
curl http://localhost:3000/api/health
```

### 测试 Chat API（前端方式）
打开 https://capwords.vercel.app 并：
1. 点击右上角 ⚙️ 设置
2. 输入 BOT_APP_KEY
3. 上传一张图片

---

## 📝 文件说明

| 文件 | 用途 |
|-----|------|
| `api/chat-sse.js` | 主 API - SSE 流式响应 |
| `api/upload-image.js` | 图片上传到腾讯云 COS |
| `api/health.js` | 健康检查 - 诊断环境变量 |
| `public/index.html` | 前端应用 |
| `package.json` | 项目依赖配置 |
| `vercel.json` | Vercel 部署配置 |

---

## 🛠️ 故障排除

### 部署日志查看
```bash
vercel logs --tail
```

### 实时调试
打开 Vercel 控制面板 → 你的项目 → Deployments → 最新部署 → View Build Logs

### 本地模拟 Vercel
```bash
npm install -g vercel
vercel dev
```

然后访问 http://localhost:3000

---

## 💡 常见问题

**Q: 为什么总是显示 "Item"？**  
A: AI 没有正确识别或解析失败。查看浏览器控制台看详细错误。

**Q: 上传图片后卡在 "卡片生成中"？**  
A: 检查：
- BOT_APP_KEY 是否正确
- 图片是否太大（> 10MB）
- 网络连接是否正常

**Q: 如何更新代码？**  
A: 修改后运行 `vercel --prod` 重新部署。

---

## ✅ 验证清单

部署前，确认以下几点：

- [ ] `package.json` 中没有 node-fetch
- [ ] `api/chat-sse.js` 使用的是原生 fetch
- [ ] `public/index.html` 有 BOT_APP_KEY 初始化
- [ ] 所有 API 文件都在 `api/` 目录中
- [ ] `vercel.json` 配置正确

---

## 📞 需要帮助？

- 查看 Vercel 部署文档：https://vercel.com/docs
- 腾讯云 SDK 文档：https://cloud.tencent.com/document/sdk/nodejs
